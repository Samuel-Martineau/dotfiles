# Dotfiles

## Install

1. `xcode-select --install`
2. `sudo xcodebuild -license accept`
3. Log in the AppStore
4. `git clone --recurse-submodules -j8 https://github.com/Samuel-Martineau/dotfiles`
5. `./install`
